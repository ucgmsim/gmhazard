from setuptools import setup, find_packages

setup(
    name="sha_calc",
    version="19.8.1",
    packages=find_packages(),
    url="https://github.com/ucgmsim/gmhazard",
    description="Generic calculation functions for seismic hazard analysis (SHA)",
)
